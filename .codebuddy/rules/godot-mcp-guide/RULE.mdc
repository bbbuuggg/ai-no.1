---
description: Godot MCP工具能力边界——40+工具的功能范围、限制条件、适合/不适合的场景、标准工作流
alwaysApply: false
enabled: true
updatedAt: 2026-04-24T14:35:00.000Z
provider:
---

# Godot MCP 能力边界参考

## 通信架构

```
AI 助手 (CodeBuddy) ↔ MCP (JSON/HTTP localhost:3100) ↔ Godot Editor (4.6)
```

## 最强能力区（直接使用）

| 模块 | 工具 | 能力 |
|------|------|------|
| 场景管理 | `scene_management` | 创建/打开/保存/关闭场景 |
| 节点操作 | `node_lifecycle` | 创建/删除/复制/实例化节点 |
| 属性设置 | `node_property` | 读写任意节点属性 |
| 变换 | `node_transform` | 位置/旋转/缩放 |
| 脚本管理 | `script_manage` + `script_attach` | 创建/读写/挂载 GDScript |
| 场景层级 | `scene_hierarchy` + `node_query` | 获取节点树/查找节点 |
| 输入映射 | `project_input` | 添加动作和绑定 |
| 文件系统 | `filesystem_*` + `resource_*` | 目录/文件/资源管理 |
| 场景运行 | `scene_run` | 运行/停止测试 |

## 中等能力区（可用但有限制）

| 模块 | 限制 |
|------|------|
| 动画系统 | 不支持骨骼动画关键帧、外部动画导入、SpriteFrames |
| 物理系统 | 关节配置较基础，射线检测仅运行时有效 |
| 材质/Shader | 不支持 VisualShader 图编辑 |
| 粒子系统 | 无法实时预览调参效果 |
| TileMap | 逐格操作效率低，大面积绘制建议编辑器手动 |

## 明确不支持

| 功能 | 替代方案 |
|------|---------|
| 导入外部图片/模型/音频到项目 | 手动拖入 Godot 文件面板 |
| VisualShader 图编辑 | 编写文本 Shader 代码 |
| 骨骼绑定 (Skeleton2D/3D) | Blender 中完成后导入 |
| 地形编辑 (Terrain3D) | 编辑器手动或插件 |
| UV 映射编辑 | Blender 等外部工具 |
| 导出游戏为可执行文件 | 编辑器 Export 菜单 |
| 运行时调试（断点/步进） | 编辑器 Debugger |
| 获取游戏画面截图 | 无法捕获渲染输出 |
| 运行时修改游戏状态 | MCP 只操作编辑器数据 |
| C# 脚本管理 | 仅支持 GDScript |

## 标准工作流

### 创建新场景
```
scene_management.create → node_lifecycle.create(相对路径) →
collision_shape.create_box → node_property.set → node_transform.set_position →
script_manage.create+write → script_attach.attach →
animation_animation.create → animation_track.add_* + add_key →
scene_management.save → 更新 commit_log.md → scene_run.play_main
```

### 修改现有场景
```
scene_management.open → scene_hierarchy.get_tree →
script_manage.read → 修改 → script_manage.write →
scene_management.save → 更新 commit_log.md → scene_run.play_current
```

## 适合 MCP 的事 vs 应手动/脚本做的事

| 适合 MCP | 应手动做 | 应用脚本代码 |
|---------|---------|------------|
| 搭建节点树 | 精细粒子调参 | 程序化生成关卡 |
| 批量设属性 | TileMap 大面积绘制 | 复杂 AI 行为树 |
| 写完整脚本 | 导入外部美术资源 | 网络多人同步 |
| 动画配置 | VisualShader 编辑 | 存档/读档系统 |
| 项目配置 | 导出游戏包 | 动态资源加载 |
| 文件管理 | — | — |
