---
description: AINo.1 Godot项目核心规范——目录结构、命名规则、输入映射、碰撞层、脚本结构、节点结构约定
alwaysApply: true
enabled: true
updatedAt: 2026-04-24T14:35:00.000Z
provider:
---

# Godot 项目核心规范

## 基础信息

- 项目名称：AINo.1
- Godot 版本：4.6
- 渲染器：Forward+
- 脚本语言：**GDScript（禁止使用 C#）**
- 主场景：`res://scenes/main.tscn`

## 目录结构

```
res://
├── scenes/              # 所有场景文件 (.tscn)
│   ├── levels/          # 关卡场景
│   ├── characters/      # 角色场景
│   └── ui/              # UI 场景
├── scripts/             # 所有 GDScript 脚本 (.gd)
│   ├── enemies/         # 敌人脚本
│   ├── systems/         # 系统脚本
│   └── utils/           # 工具函数
├── assets/
│   ├── sprites/         # 2D 贴图、精灵表
│   ├── models/          # 3D 模型 (.glb, .gltf)
│   ├── audio/           # 音效和音乐
│   └── fonts/           # 字体文件
├── ui/                  # UI 专用资源（主题、样式）
├── autoload/            # 全局单例脚本
├── resources/           # .tres 资源文件
└── addons/              # 编辑器插件
```

**禁止**在项目根目录直接放置 `.gd` 或 `.tscn` 文件。

## 命名规范

- **节点命名**：PascalCase → `Player`, `EnemyGoblin`, `HealthBar`
- **变量/函数**：snake_case → `move_speed`, `take_damage()`
- **常量**：UPPER_SNAKE_CASE → `MAX_HEALTH`, `JUMP_VELOCITY`
- **信号**：snake_case → `health_changed`, `died`
- **文件名**：snake_case → `player.gd`, `main_menu.tscn`

## 输入映射

**禁止使用 `ui_*` 系列内置动作控制角色行为。** 所有游戏逻辑必须使用以下自定义动作名：

| 动作名 | 主键 | 备选键 | 用途 |
|--------|------|--------|------|
| `move_left` | A | Left | 向左移动 |
| `move_right` | D | Right | 向右移动 |
| `move_up` | W | Up | 向上移动 |
| `move_down` | S | Down | 向下移动 |
| `jump` | Space | — | 跳跃 |
| `attack` | Mouse Left | J | 攻击 |
| `interact` | E | F | 交互 |
| `dash` | Shift | — | 冲刺 |
| `pause` | Escape | — | 暂停 |
| `inventory` | Tab | I | 背包 |

```gdscript
# ✅ 正确
var direction = Input.get_axis("move_left", "move_right")
# ❌ 禁止
var direction = Input.get_axis("ui_left", "ui_right")
```

## 碰撞层

| 层 | 名称 | 用途 |
|----|------|------|
| 1 | Player | 玩家角色 |
| 2 | Enemy | 敌人 |
| 3 | Terrain | 地形、平台、墙壁 |
| 4 | Projectile | 子弹、投射物 |
| 5 | Pickup | 可拾取物品 |
| 6 | Trigger | 触发区域 |

## 场景节点结构约定

### 2D 角色
```
Player (CharacterBody2D)
├── Sprite2D
├── CollisionShape2D
├── AnimationPlayer
├── HitboxArea (Area2D) → CollisionShape2D
└── HurtboxArea (Area2D) → CollisionShape2D
```

### 3D 角色
```
Player (CharacterBody3D)
├── MeshInstance3D
├── CollisionShape3D
├── AnimationPlayer
├── Camera3D
└── NavigationAgent3D
```

## 脚本结构顺序

```gdscript
extends CharacterBody2D
class_name Player           # 可选

signal health_changed(value: int)      # 信号
@export var max_health: int = 100      # 导出变量
const JUMP_VELOCITY = -400.0           # 常量
var current_health: int = 100          # 成员变量
@onready var sprite = $Sprite2D        # @onready 引用

func _ready() -> void:                 # 生命周期
func _physics_process(delta) -> void:
func take_damage(amount: int) -> void: # 公共方法
func _update_anim() -> void:           # 私有方法
func _on_area_entered(body) -> void:   # 信号回调
```

## AutoLoad 单例约定

| 名称 | 路径 | 用途 |
|------|------|------|
| GameManager | `res://autoload/game_manager.gd` | 游戏状态、场景切换 |
| AudioManager | `res://autoload/audio_manager.gd` | 音频管理 |
| SaveManager | `res://autoload/save_manager.gd` | 存档读档 |
| EventBus | `res://autoload/event_bus.gd` | 全局事件总线 |

仅在需要时创建，不必一开始全部创建。
