---
description: Godot MCP操作安全规则——9条实战陷阱和操作前检查清单，防止节点丢失、引用断裂、输入失效等问题
alwaysApply: true
enabled: true
updatedAt: 2026-04-24T14:35:00.000Z
provider:
---

# MCP 操作安全规则

## 操作前检查清单

**AI 在每次执行 MCP 操作前，必须过一遍此清单：**

| 操作类型 | 检查项 |
|---------|--------|
| 创建节点 | parent_path 是否使用相对路径（`.` 或 `Player`）？ |
| 创建 CollisionShape | 是否紧接着设置了具体形状？ |
| 创建动画 | 是否添加了轨道和至少 2 个关键帧？ |
| 移动/重命名文件 | 是否更新了所有场景中的引用？ |
| 修改脚本 | 是否先 read 再 write？ |
| 修改项目设置 | 是否需要重启游戏才能生效？ |
| 完成一组操作 | 是否调用了 `scene_management.save`？ |
| 运行测试前 | 是否已保存所有更改？ |
| **任何修改完成后** | **是否已更新 `commit_log.md`？（强制）** |

---

## 陷阱 1：节点创建路径错误导致不保存

```
❌ parent_path: "/root" 或 "/root/Main"
   → 节点挂载到运行时 SceneTree，保存后丢失。

✅ parent_path: "."（场景根节点）或 "Player"（子节点）
   → 节点属于 .tscn 文件，持久存在。

🔍 创建后调用 scene_hierarchy.get_tree 验证。
```

## 陷阱 2：移动资源后引用断裂

```
❌ resource_manage.move 移动文件后不更新引用
   → "File not found" 错误。

✅ 移动后必须执行：
   1. scene_management.open → 打开引用该资源的场景
   2. script_attach.detach → 卸载旧路径
   3. script_attach.attach → 挂载新路径
   4. scene_management.save → 保存
   移动 .tscn 时还需更新 project_settings 的 main_scene。
```

## 陷阱 3：使用内置 ui_* 动作控制角色

```
❌ Input.get_axis("ui_left", "ui_right")
   → A/D 键被 UI 焦点系统拦截，只有方向键有效。

✅ Input.get_axis("move_left", "move_right")
   → 使用自定义动作，无冲突。
```

## 陷阱 4：CollisionShape 未设置形状

```
❌ 只创建 CollisionShape2D 节点不设置形状
   → 运行时警告 "A shape must be provided"，碰撞无效。

✅ 创建后立即调用 create_box / create_sphere / create_capsule。
```

## 陷阱 5：动画只建资源没添加轨道和关键帧

```
❌ animation_animation.create 后就结束
   → 动画播放时无任何视觉效果。

✅ 必须完成：
   1. add_property_track → 添加属性轨道
   2. add_key × 2+ → 至少 2 个关键帧
   3. set_loop → 按需设置循环
```

## 陷阱 6：脚本 write 覆盖丢失内容

```
❌ 直接 script_manage.write 写入新内容
   → 丢失用户手动修改的代码。

✅ 先 script_manage.read → 修改 → script_manage.write。
```

## 陷阱 7：忘记保存场景

```
❌ 创建节点、设置属性后直接运行或关闭编辑器
   → 所有修改丢失。

✅ 每完成一个功能模块就调用 scene_management.save。
   保存时机：创建完节点后、挂载脚本后、配置动画后、运行测试前。
```

## 陷阱 8：项目设置修改后未重启

```
❌ 添加输入动作后在运行中的游戏里直接测试
   → 新动作不存在。

✅ 修改后：scene_run.stop → scene_run.play_main 重启。
```

## 陷阱 9：运行时 MCP 修改不影响游戏实例

```
⚠️ MCP 操作的是编辑器数据，不是运行中的游戏。

✅ 正确流程：stop → 修改 → save → play_main。
```
