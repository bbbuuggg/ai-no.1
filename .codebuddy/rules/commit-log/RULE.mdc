---
description: commit_log.md强制更新规则——每次代码修改后必须同步更新项目进度文档
alwaysApply: true
enabled: true
updatedAt: 2026-04-24T14:35:00.000Z
provider:
---

# commit_log 强制更新规则

> **这是最高优先级的强制性步骤，不可跳过。**

## 什么是 commit_log

`commit_log.md` 是项目根目录下的进度快照文档，包含三个部分：

1. **第一部分**：项目当前完整状态（目录结构、场景节点树、脚本清单、输入映射、动画配置）
2. **第二部分**：按时间倒序排列的操作变更日志
3. **第三部分**：已知问题与待办事项

核心目的：**让新打开的 AI 对话窗口仅通过阅读此文件就能完全了解项目进度，无需额外上下文。**

## 何时必须更新

以下任何操作完成后，**必须立即**更新 `commit_log.md`：

- 创建/删除/修改场景 → 更新场景节点树
- 创建/修改/移动脚本 → 更新脚本清单
- 添加/修改输入映射 → 更新输入映射表
- 创建/修改动画 → 更新动画配置
- 创建/删除目录或文件 → 更新目录结构
- 修改项目设置 → 更新项目基础信息
- **以上任何操作** → **在第二部分顶部追加变更日志条目**
- 发现新问题 → 更新第三部分
- 完成待办 → 更新待办状态

## 变更日志条目格式

```markdown
#### 操作 N：简要描述
- **创建/修改/删除/修复** `文件路径`：具体做了什么
- **问题**（如有）：遇到了什么问题
- **修复**（如有）：如何解决的
```

## 在操作流程中的固定位置

```
1. 执行功能开发 / bug 修复
2. scene_management.save → 保存场景
3. ✅ 更新 commit_log.md → 记录变更（不可跳过）
4. scene_run.play_main → 运行测试（可选）
```

## 新对话窗口的启动流程

```
1. 读取 commit_log.md → 了解项目当前状态和最近变更
2. 开始接受用户指令并执行
```
